no matching files found
Usage: [options] file/dir
	 FoLiA-txt will produce FoLiA files from text files 
	 The output will only contain <p> and <str> nodes.
	-t <threads>
	--threads <threads> Number of threads to run on.
			 If 'threads' has the value "max", the number of threads is set to a
			 reasonable value. (OMP_NUM_TREADS - 2)
	-h or --help	 this message
	-V or --version	 show version 
	-O	 output directory 
	--remove-end-hyphens:yes|no (default = 'yes') 
			 if 'yes', hyphens (-) att the end of lines are converted to <t-hbr> nodes.
			 And ignored in general.
	--setname The FoLiA setname of the created nodes. (Default 'FoLiA-txt-set')
	--class The classname of the <t> nodes that are created. (Default 'FoLiA-txt')
foliautils 0.23
Usage: [options] file/dir
	 FoLiA-txt will produce FoLiA files from text files 
	 The output will only contain <p> and <str> nodes.
	-t <threads>
	--threads <threads> Number of threads to run on.
			 If 'threads' has the value "max", the number of threads is set to a
			 reasonable value. (OMP_NUM_TREADS - 2)
	-h or --help	 this message
	-V or --version	 show version 
	-O	 output directory 
	--remove-end-hyphens:yes|no (default = 'yes') 
			 if 'yes', hyphens (-) att the end of lines are converted to <t-hbr> nodes.
			 And ignored in general.
	--setname The FoLiA setname of the created nodes. (Default 'FoLiA-txt-set')
	--class The classname of the <t> nodes that are created. (Default 'FoLiA-txt')

no file or dir specified!
Usage: FoLiA-2text [options] file/dir
	 FoLiA-2text will produce a text from a FoLiA file, 
	 or a whole directory of FoLiA files 
	-c OR --class='name'	 use 'name' as the folia class for <t> nodes. (default is 'current')
	--retaintok	 Retain tokenization. Default is attempt to remove.
	--restore-formatting 	Attempt to restore the original formatting.
			 Will insert (soft-)hypens and such.
	-t 'threads' or
	--threads='threads' Number of threads to run on.
			 If 'threads' has the value "max", the number of threads is set to a
			 reasonable value. (OMP_NUM_TREADS - 2)
	-v increment the verbosity level.
	-h or --help	 this message
	-V or --version 	 show version 
	-e		 expr: specify the expression all input files should match with.
	-o		 name of the output file(s) prefix.
foliautils 0.21
Usage: FoLiA-2text [options] file/dir
	 FoLiA-2text will produce a text from a FoLiA file, 
	 or a whole directory of FoLiA files 
	-c OR --class='name'	 use 'name' as the folia class for <t> nodes. (default is 'current')
	--retaintok	 Retain tokenization. Default is attempt to remove.
	--restore-formatting 	Attempt to restore the original formatting.
			 Will insert (soft-)hypens and such.
	-t 'threads' or
	--threads='threads' Number of threads to run on.
			 If 'threads' has the value "max", the number of threads is set to a
			 reasonable value. (OMP_NUM_TREADS - 2)
	-v increment the verbosity level.
	-h or --help	 this message
	-V or --version 	 show version 
	-e		 expr: specify the expression all input files should match with.
	-o		 name of the output file(s) prefix.
Hij is tegen de lamp gelopen. Met een flinke hoofdwond tot gevolg.

De tekst gaat verder hier.
Wie had dat gedacht?

Totaal iets anders.
Dit is hoofdstuk 1.

Dit is paragraaf 1.

Hij is tegen de lamp gelopen. Met een flinke hoofdwond tot gevolg.

Dit is paragraaf 2.

Een tekst in paragraaf 3.


Dit is hoofdstuk 2.

Een tekst in paragraaf 1 van hoofdstuk 2.
Wie had dat gedacht?

Totaal iets anders.
Wie had dat gedacht?

Totaal iets anders.
het creëren van
het van
het van
het van
het van
het CREEREN van
het creëren van
het creeren van
het CREEREN van
Dit is een test. Met enkele afgebroken zin
nen. Dit is de eerste. Met ook een soft af
breking.
En dit heeft een lange' 'space val.
En dit heeft een enkele' 'space val.
En dit heeft een speciale' 'space val.

Dit is een test. Met enkele afgebroken zin-

nen. Dit is de eerste. Met ook een soft af¬

breking.
En dit heeft een lange'       'space val.
En dit heeft een enkele' 'space val.
En dit heeft een speciale'⍽'space val.

